target_query_id:
  type: integer
  default: 0
  description: "The ID of the Data Explorer query where you want to hide a parameter"

parameter_to_hide:
  type: string
  default: ""
  description: "The name of the parameter field you want to hide (e.g. username_search)"